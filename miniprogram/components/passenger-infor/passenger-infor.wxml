<view class="passenger-infor-wraper" bindlongtap="deletePassenger" data-passenger="{{passenger}}">
    <view class="id">{{num}}</view>
    <view class="start">{{passenger.startPoint.title}}</view>
    <view class="end">{{passenger.endPoint.title}}</view>
</view>
